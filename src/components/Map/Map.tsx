import { FC } from 'react';

interface MapProps {
    x: number;
    y: number;
}

const Map: FC<MapProps> = (props) => {
    return (
        <div>
            Map
        </div>
    )
}

export default Map;